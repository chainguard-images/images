contents:
  packages:
    - alpine-keys
    - bubblewrap
    - melange
    - wolfi-keys

paths:
  - path: /work
    type: directory
    permissions: 0o777

work-dir: /work

entrypoint:
  command: /usr/bin/melange
cmd: --help

