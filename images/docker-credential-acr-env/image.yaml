versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: docker-credential-acr-env
      subvariants:
        - suffix: -dev
          options:
            - dev
