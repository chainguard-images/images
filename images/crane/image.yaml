# Opt-in to the new terraform-based release
terraform: true

status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: crane
      subvariants:
        - suffix: -dev
          options:
            - dev
