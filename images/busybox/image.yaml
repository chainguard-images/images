versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: busybox
  - apko:
      config: configs/latest-musl.apko.yaml
      extractTagsFrom:
        package: busybox
        prefix: musl-
