status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: clang-15
      subvariants:
        - suffix: -dev
          options:
            - dev