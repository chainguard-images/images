status: experimental

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: telegraf
      subvariants:
        - suffix: -dev
          options:
            - dev
