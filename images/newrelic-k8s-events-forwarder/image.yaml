# Opt-out of the new terraform-based release
terraform: false

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: newrelic-k8s-events-forwarder
      subvariants:
        - suffix: -dev
          options:
            - dev
