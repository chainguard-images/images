contents:
  repositories:
    - https://packages.wolfi.dev/os
  packages:
    - wolfi-base
    - wolfi-baselayout

cmd: /bin/sh -l
  
# optional environment configuration
environment:
  PATH: /usr/sbin:/sbin:/usr/bin:/bin
