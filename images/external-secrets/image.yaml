versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: external-secrets-operator
      subvariants:
        - suffix: -dev
          options:
            - dev
