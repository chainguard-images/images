contents:
  packages:
    - alpine-keys
    - wolfi-base
    - alpine-keys
    - bubblewrap
    - melange

paths:
  - path: /work
    type: directory
    permissions: 0o777

work-dir: /work

entrypoint:
  command: /usr/bin/melange
cmd: --help

