versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: oidc-discovery-provider
      subvariants:
        - suffix: -dev
          options:
            - dev
