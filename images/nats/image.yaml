status: experimental
versions:
  - apko:
      extractTagsFrom:
        package: nats-server
      config: configs/latest.apko.yaml
  - apko:
      extractTagsFrom:
        package: nats-server
        prefix: dev-
      config: configs/latest-dev.apko.yaml
