status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: consul
      subvariants:
        - suffix: -dev
          options:
            - dev
        - suffix: -compat
          options:
            - consul-oci-entrypoint-compat
