contents:
  packages:
    - wolfi-base

#Create a nonroot user but run as root by default
accounts:
  groups:
    - groupname: nonroot
      gid: 65532
  users:
    - username: nonroot
      uid: 65532

cmd: /bin/sh -l
