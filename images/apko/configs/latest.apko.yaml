contents:
  packages:
    - wolfi-base
    - alpine-keys
    - apko

paths:
  - path: /work
    type: directory
    permissions: 0o777

work-dir: /work

entrypoint:
  command: /usr/bin/apko
cmd: --help

