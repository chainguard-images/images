status: experimental

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: rqlited
      subvariants:
        - suffix: -dev
          options:
            - dev
