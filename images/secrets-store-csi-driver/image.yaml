status: experimental

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: secrets-store-csi-driver
      subvariants:
        - suffix: -dev
          options:
            - dev
