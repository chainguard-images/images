versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: flux-image-automation-controller
      subvariants:
        - suffix: -dev
          options:
            - dev