versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: nginx
      subvariants:
        - suffix: -dev
          options:
            - dev
  - apko:
      config: configs/next.apko.yaml
