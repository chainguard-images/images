versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: kubernetes-csi-external-provisioner
      subvariants:
        - suffix: -dev
          options:
            - dev
