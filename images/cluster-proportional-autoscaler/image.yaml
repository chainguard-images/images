versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: cluster-proportional-autoscaler
      subvariants:
        - suffix: -dev
          options:
            - dev
