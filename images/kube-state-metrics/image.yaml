# Opt-out of the new terraform-based release
terraform: false

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: kube-state-metrics
      subvariants:
        - suffix: -dev
          options:
            - dev
