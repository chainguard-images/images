versions:
  - apko:
      extractTagsFrom:
        package: php
      config: configs/latest.apko.yaml
  - apko:
      extractTagsFrom:
        package: php
        prefix: dev-
      config: configs/latest-dev.apko.yaml
