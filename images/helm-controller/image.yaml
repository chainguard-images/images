status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: flux-helm-controller
      subvariants:
        - suffix: -dev
          options:
            - dev