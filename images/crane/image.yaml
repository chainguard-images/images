status: experimental

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: crane
      subvariants:
        - suffix: -dev
          options:
            - dev
