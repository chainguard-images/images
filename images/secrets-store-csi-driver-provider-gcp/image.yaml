# Opt-out of the new terraform-based release
terraform: false

status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: secrets-store-csi-driver-provider-gcp
      subvariants:
        - suffix: -dev
          options:
            - dev
