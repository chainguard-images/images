status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      subvariants:
        - suffix: -dev
          options:
            - dev
      extractTagsFrom:
        package: dex
