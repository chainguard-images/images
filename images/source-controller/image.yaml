status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: flux-source-controller
      subvariants:
        - suffix: -dev
          options:
            - dev