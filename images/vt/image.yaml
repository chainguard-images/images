# Opt-out of the new terraform-based release
terraform: false

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: vt-cli
      subvariants:
        - suffix: -dev
          options:
            - dev
