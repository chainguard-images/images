versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: kubernetes-csi-external-resizer
      subvariants:
        - suffix: -dev
          options:
            - dev
