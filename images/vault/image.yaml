# Opt-out of the new terraform-based release
terraform: false

status: stable
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: vault
      subvariants:
        - suffix: -dev
          options:
            - dev
