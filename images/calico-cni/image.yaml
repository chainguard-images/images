status: stable
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: calico-node
      subvariants:
        - suffix: -dev
          options:
            - dev
            - calico-dev

options:
  calico-dev:
    contents:
      packages:
        add:
          - calicoctl
