# Opt-out of the new terraform-based release
terraform: false

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: newrelic-nri-kube-events
      subvariants:
        - suffix: -dev
          options:
            - dev
