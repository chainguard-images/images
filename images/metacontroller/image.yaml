# Opt-out of the new terraform-based release
terraform: false

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: metacontroller
      subvariants:
        - suffix: -dev
          options:
            - dev
