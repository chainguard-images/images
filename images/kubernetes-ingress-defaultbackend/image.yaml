versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: kubernetes-ingress-defaultbackend
      subvariants:
        - suffix: -dev
          options:
            - dev
