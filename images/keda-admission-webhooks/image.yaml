versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: keda-admission-webhooks
      subvariants:
        - suffix: -dev
          options:
            - dev
