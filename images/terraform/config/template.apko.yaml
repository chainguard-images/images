contents:
  packages:
    # terraform comes in via var.extra_packages

accounts:
  groups:
    - groupname: nonroot
      gid: 65532
  users:
    - username: nonroot
      uid: 65532
  run-as: 65532

entrypoint:
  command: /usr/bin/terraform

cmd: --help
