status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: cert-manager-webhook
      subvariants:
        - suffix: -dev
          options:
            - dev
            - cert-manager-dev

options:
  cert-manager-dev:
    contents:
      packages:
        add:
          - cmctl