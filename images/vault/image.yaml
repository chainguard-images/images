status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: vault
      subvariants:
        - suffix: -dev
          options:
            - dev
