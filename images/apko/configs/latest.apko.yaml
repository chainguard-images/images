contents:
  packages:
    - alpine-keys
    - apko
    - wolfi-keys

paths:
  - path: /work
    type: directory
    permissions: 0o777

work-dir: /work

entrypoint:
  command: /usr/bin/apko

cmd: --help
