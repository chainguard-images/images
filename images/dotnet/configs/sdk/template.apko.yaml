contents:
  packages:
    - busybox
    # dotnet comes via var.extra_packages

accounts:
  groups:
    - groupname: nonroot
      gid: 65532
  users:
    - username: nonroot
      uid: 65532
  run-as: 65532

environment:
  DOTNET_CLI_TELEMETRY_OPTOUT: "1"

cmd: /bin/sh -l
