status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: vault-k8s
      subvariants:
        - suffix: -dev
          options:
            - dev
