status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: k8s-sidecar
      subvariants:
        - suffix: -dev
          options:
            - dev
