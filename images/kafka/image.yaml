status: experimental
versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: kafka
      subvariants:
        - suffix: -dev
          options:
            - dev
