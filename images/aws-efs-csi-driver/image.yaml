# Opt-out of the new terraform-based release
terraform: false

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: aws-efs-csi-driver
      subvariants:
        - suffix: -dev
          options:
            - dev
