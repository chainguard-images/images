versions:
  - apko:
      extractTagsFrom:
        package: python3
      config: configs/latest.apko.yaml
  - apko:
      extractTagsFrom:
        package: python3
        prefix: dev-
      config: configs/latest-dev.apko.yaml
  - apko:
      extractTagsFrom:
        package: python3
        prefix: musl-
      config: configs/latest-musl.apko.yaml
  - apko:
      extractTagsFrom:
        package: python3
        prefix: musl-dev-
      config: configs/latest-musl-dev.apko.yaml
