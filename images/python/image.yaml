versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: python3
  - apko:
      config: configs/latest-glibc.apko.yaml
      extractTagsFrom:
        package: python3
        prefix: glibc-
