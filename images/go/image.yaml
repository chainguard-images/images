versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: go
  - apko:
      config: configs/latest-glibc.apko.yaml
      extractTagsFrom:
        package: go
        prefix: glibc-
