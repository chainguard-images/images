versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: kubernetes-dashboard-metrics-scraper
      subvariants:
        - suffix: -dev
          options:
            - dev
