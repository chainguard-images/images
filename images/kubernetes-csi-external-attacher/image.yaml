versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: kubernetes-csi-external-attacher
      subvariants:
        - suffix: -dev
          options:
            - dev
