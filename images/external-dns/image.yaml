status: experimental

versions:
  - apko:
      config: configs/latest.apko.yaml
      extractTagsFrom:
        package: external-dns
      subvariants:
        - suffix: -dev
          options:
            - dev
